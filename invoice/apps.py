"""Configuration for invoice app."""
from django.apps import AppConfig


class InvoiceConfig(AppConfig):
    """Configuration for invoice app."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'invoice'
